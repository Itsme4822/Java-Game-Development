package ca.jam.gameobject;

public enum ObjectId {
	/*
	 * This is where we will keep all the ids of every object in the game (players, enemies, weapons, etc.)
	 * This is a list of all the ids in the game.
	 * Example of how to use:
	 * 		player(),
	 * 		dog(),
	 * 		...
	 * 		enemy();
	 */
	testObject(),
	player(),
	floor();
}
